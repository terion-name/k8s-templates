apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: app
spec:
  rules:
    - host: host
      http:
        paths:
          - path: /
            backend:
              serviceName: app
              servicePort: 3000
